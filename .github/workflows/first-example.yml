name: my first workflows

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "my first github action job"
        - name: list files
          run: ls
        - name: Read files
          run: ../cat README.md
